content:
  application/json:
    schema:
      type: object
      items:
        $ref: ../schemas/SemanticPatchOperation.yaml
description: >-
  Requires a Semantic Patch representation of the desired changes to the
  resource.
  'https://apidocs.launchdarkly.com/reference#updates-via-semantic-patches'. The
  addition of comments is also supported.
required: true
