content:
  application/json:
    schema:
      type: array
      items:
        $ref: ../schemas/PatchOperation.yaml
description: >-
  Requires a JSON Patch representation of the desired changes to the project.
  'http://jsonpatch.com/' Feature flag patches also support JSON Merge Patch
  format. 'https://tools.ietf.org/html/rfc7386' The addition of comments is also
  supported.
required: true
