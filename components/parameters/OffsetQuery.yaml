name: offset
in: query
required: false
description: >-
  Where to start in the list. This is for use with pagination. For example, an
  offset of 10 would skip the first 10 items and then return the next limit
  items.
schema:
  type: number
