type: object
properties:
  instructions:
    type: array
    items:
      properties:
        kind:
          type: string
          example: removeUserTargets
          description: >-
            The name of the modification you would like to perform on a
            resource.
        conflicts:
          type: array
          items:
            $ref: ./ScheduledChangesFeatureFlagConflict.yaml
